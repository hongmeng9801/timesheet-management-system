# 阿里云OSS部署配置
# 请将此文件重命名为 .env 并填入真实的密钥信息

# 阿里云AccessKey (必填)
ALIYUN_ACCESS_KEY_ID=your_access_key_id_here
ALIYUN_ACCESS_KEY_SECRET=your_access_key_secret_here

# 腾讯云密钥 (可选，如果使用腾讯云)
TENCENT_SECRET_ID=your_secret_id_here
TENCENT_SECRET_KEY=your_secret_key_here

# 部署配置说明：
# 1. 访问 https://ram.console.aliyun.com/users 创建RAM用户
# 2. 为用户添加 AliyunOSSFullAccess 权限
# 3. 创建AccessKey并填入上方配置
# 4. 在OSS控制台创建名为 timesheet-mobile-app 的存储桶
# 5. 设置存储桶为公共读权限